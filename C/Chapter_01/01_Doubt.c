#include<stdio.h>
int main() {
    int  a;
    scanf("%d", &a);
    printf("the value of a is %d", a);
    printf("checking for git modification status");
    printf("again cheching for git modification status");
    return 0;
}