#include<stdio.h>
    int main(){
        int x;
        int y;
        int n=1;
        int z;

        printf("Enter the number: ");
        scanf("%d", &x);
        
        while(x!=0){
            z=x/10;

        }
        printf("The number of digits in %d is: %d", x, z);

        return 0;

        // To be continued...
    }
